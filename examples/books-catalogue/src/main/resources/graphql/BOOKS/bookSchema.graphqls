extend type  Query
{
    books: [Book]
    bookbyId(id: ID!): Book
    booksByAuthor(id: ID!): [Book]
    booksByPublisher(id: ID!): [Book]
}

type Book
{
    id: ID!
    title: String!
    genre: String!
    author: Author
    publisher: Publisher
    createdBy: ID
    updatedBy: ID
}