plugins {}

dependencies {
    // For easier Specification Search
    api "org.hibernate.orm:hibernate-jpamodelgen"
    api "org.hibernate.orm:hibernate-jpamodelgen"
    api "org.hibernate.orm:hibernate-jpamodelgen"
}